sources:
  test-tco79:
    description: 5km experiment, coupuled with IFS
    driver: yaml_file_cat
    args:
      path: '{{CATALOG_DIR}}/test-tco79.yaml'
  tco79-orca1:
    description: tco79-orca1 data
    driver: yAaml_file_cat
    args:
      path: '{{CATALOG_DIR}}/tco79-orca1.yaml'
  tco399-eORCA025:
    description: tco399-eORCA025 data
    driver: yaml_file_cat
    args:
      path: '{{CATALOG_DIR}}/tco399-eORCA025.yaml'
