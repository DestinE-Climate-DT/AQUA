# Choice of interface
interface: AQUA
# Choice of model name. It is printed in the output table
model:
  name: AQUA

dirs:
  exp: '' #not necessary we use xarray dataset instead of files
  clm: /gpfs/scratch/dese28/dese28422/ecmean_files/climatology
  tab: /gpfs/scratch/dese28/dese28422/ecmean_files/YAML
  fig: /gpfs/scratch/dese28/dese28422/ecmean_files/figs

global:
  atm_vars: [psl,tas,pr]
  # [tas, psl, pr, evspsbl, pme, cll, clm, clh,
  #           pr_oce, pme_oce, pr_land, pme_land,
  #           rsnt, rlnt, net_toa, rsns, rlns, hfss, hfls, net_sfc_nosn]
  oce_vars: [] #[tos, sos]
  ice_vars: [] #[siconc]
  tab_vars: []
  regions: ['Global', 'North Midlat', 'Tropical', 'South Midlat']
  seasons: ['ALL', 'DJF', 'JJA']

PI:
  2d_vars:
    field: [psl,tas,pr]
  3d_vars:
    field: [ta, ua, va, hus]
  oce_vars:
    field: [ sos] #[tos, sos]
  ice_vars:
    field: [] #[siconc]
  regions: ['Global', 'North Midlat', 'Tropical', 'South Midlat']
  seasons: ['ALL', 'DJF', 'JJA']
  resolution: r360x180

