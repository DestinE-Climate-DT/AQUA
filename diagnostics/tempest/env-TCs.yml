# Create the tropical-rainfall environment:
#   conda env create -f env-tropical-rainfall.yml
# Alternatively, use mamba
#mamba env create -f env-TCs.yml

name : TCs
channels:
  - conda-forge
dependencies:
  - python=3.9
  - pyYAML
  - xarray
  - intake
  - dask
  - numpy
  - jinja2
  - tqdm
  - udunits2
  - zarr
  - netcdf4
  - intake-xarray
  - ecCodes      
  - matplotlib
  - cartopy
  - healpy
  - netcdf4
  - ipykernel
  - cfunits
  - tempest-extremes
  - cdo
  - python-cdo
  - pip
  - pip:
    - sparse==0.13.0
    - docker
    - gribscan

