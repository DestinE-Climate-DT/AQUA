# Create the ECmean4 environment:
# mamba env create -f environment.yml 

name: aqua
channels:
  - conda-forge
#script: python requirements/conda_req_for_yml.py
dependencies: 
  - python>=3.9,<3.11
  - pip
  - libnetcdf<=4.8.1
  - netcdf4
  - matplotlib
  - cartopy
  - cfunits
  - cdo
  - ipykernel
  - healpy
  # these below are only required for running notebooks
  - pip:
    - -r file:/work/users/nnazarova/AQUA/requirements/pip_requirements.txt
    # pip install -e of AQUA itself (this will bring in also smmregrid)
    - -e .
