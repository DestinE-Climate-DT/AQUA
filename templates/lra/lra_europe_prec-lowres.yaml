# templates for LRA generation. Please modify everything between {{ }} according to your configuration

#This include the option for the regridding
target:
  resolution: r050
  frequency: daily
  catalog: climatedt-phase1
  region:
    name: Europe
    lat: [25, 70]
    lon: [-30, 40]

paths:
  outdir: /scratch/project_462000911/mnurisso/prec_italy
  tmpdir: /scratch/project_462000911/mnurisso/lra_tmp

options:
  loglevel: INFO
  zarr: False
  verify_zarr: False

data:
  IFS-FESOM:
    #    story-2017-historical:
    #      hourly-hpz9-atm2d:
    #        vars: ['skt', 'msl']
    #        workers: 12
    story-2017-control:
      hourly-hpz9-atm2d:
        vars: ['skt', 'msl']
        workers: 8
    story-2017-T2K:
      hourly-hpz9-atm2d:
        vars: ['skt', 'msl']
        workers: 8
